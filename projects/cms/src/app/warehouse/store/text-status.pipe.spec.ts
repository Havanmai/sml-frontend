import { TextStatusPipe } from './text-status.pipe';

describe('TextStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TextStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
