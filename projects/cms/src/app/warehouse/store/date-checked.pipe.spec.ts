import { DateCheckedPipe } from './date-checked.pipe';

describe('DateCheckedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCheckedPipe();
    expect(pipe).toBeTruthy();
  });
});
