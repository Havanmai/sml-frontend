import { LevelPostPipe } from './level-post.pipe';

describe('LevelPostPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelPostPipe();
    expect(pipe).toBeTruthy();
  });
});
