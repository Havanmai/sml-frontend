import { TextPostofficeDetailPipe } from './text-postoffice-detail.pipe';

describe('TextPostofficeDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new TextPostofficeDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
