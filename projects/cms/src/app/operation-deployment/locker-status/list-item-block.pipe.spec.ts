import { ListItemBlockPipe } from './list-item-block.pipe';

describe('ListItemBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new ListItemBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
