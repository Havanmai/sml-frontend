import { DisplayMarginLockerPipe } from './display-margin-locker.pipe';

describe('DisplayMarginLockerPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMarginLockerPipe();
    expect(pipe).toBeTruthy();
  });
});
