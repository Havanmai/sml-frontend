import { TextStatusDetailPipe } from './text-status-detail.pipe';

describe('TextStatusDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new TextStatusDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
