import { ColorStatusDetailPipe } from './color-status-detail.pipe';

describe('ColorStatusDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorStatusDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
