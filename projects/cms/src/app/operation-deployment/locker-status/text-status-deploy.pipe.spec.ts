import { TextStatusDeployPipe } from './text-status-deploy.pipe';

describe('TextStatusDeployPipe', () => {
  it('create an instance', () => {
    const pipe = new TextStatusDeployPipe();
    expect(pipe).toBeTruthy();
  });
});
