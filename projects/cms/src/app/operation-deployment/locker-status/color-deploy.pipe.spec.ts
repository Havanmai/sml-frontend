import { ColorDeployPipe } from './color-deploy.pipe';

describe('ColorDeployPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorDeployPipe();
    expect(pipe).toBeTruthy();
  });
});
