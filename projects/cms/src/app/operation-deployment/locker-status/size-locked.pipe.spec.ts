import { SizeLockedPipe } from './size-locked.pipe';

describe('SizeLockedPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeLockedPipe();
    expect(pipe).toBeTruthy();
  });
});
