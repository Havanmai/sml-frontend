import { TaskStatusColorPipe } from './task-status-color.pipe';

describe('TaskStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
