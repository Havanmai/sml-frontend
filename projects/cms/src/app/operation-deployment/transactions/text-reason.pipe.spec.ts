import { TextReasonPipe } from './text-reason.pipe';

describe('TextReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new TextReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
