import { TextDetailStatusPipe } from './text-detail-status.pipe';

describe('TextDetailStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TextDetailStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
