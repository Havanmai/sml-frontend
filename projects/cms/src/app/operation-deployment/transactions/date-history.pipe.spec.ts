import { DateHistoryPipe } from './date-history.pipe';

describe('DateHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new DateHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
