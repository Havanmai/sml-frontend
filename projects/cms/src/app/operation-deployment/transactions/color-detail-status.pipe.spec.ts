import { ColorDetailStatusPipe } from './color-detail-status.pipe';

describe('ColorDetailStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorDetailStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
