import { TypeLockerNamePipe } from './type-locker-name.pipe';

describe('TypeLockerNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeLockerNamePipe();
    expect(pipe).toBeTruthy();
  });
});
