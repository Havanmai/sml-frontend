import { VandonTypePipe } from './vandon-type.pipe';

describe('VandonTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VandonTypePipe();
    expect(pipe).toBeTruthy();
  });
});
