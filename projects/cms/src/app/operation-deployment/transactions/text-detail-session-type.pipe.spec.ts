import { TextDetailSessionTypePipe } from './text-detail-session-type.pipe';

describe('TextDetailSessionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TextDetailSessionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
